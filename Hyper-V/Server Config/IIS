# Installasjon av IIS på Windows Server
Install-WindowsFeature Web-Server -IncludeManagementTools 

# Sjekk at webbserveren kjører. Du vil få en respons fra websiden.
Invoke-WebRequest http://nrk.no

Invoke-WebRequest |Get-Member


Invoke-WebRequest -Headers  -Uri vg.no